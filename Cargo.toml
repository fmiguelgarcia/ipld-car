[package]
name = "ipfs-unixfs"
version = "0.1.0"
edition = "2021"

[dependencies]
bytes = "1.8.0"
derive_builder = "0.20.2"
derive_more = { version = "1.0.0", features = ["from"] }
libipld = "0.16.0"
prost = "0.13.3"
tracing = "0.1.40"

[build-dependencies]
prost-build = "0.13.3"

[dev-dependencies]
test-case = "3.3.1"
test-log = { version = "0.2.16", features = ["trace"] }
